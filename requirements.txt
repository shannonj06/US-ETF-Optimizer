# Web app
streamlit==1.58.0

# Data & numerics
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.9.0

# Market data
yfinance==1.4.1
requests==2.34.2

# Optimization
cvxpy==1.9.1
PyPortfolioOpt==1.6.0

# Charts (used for the in-app figures and the PDF export)
matplotlib==3.10.9

# Database / config (data automation)
pymongo==4.10.1       # 3.12 breaks on Python 3.14 (Streamlit Cloud) -> ServerSelectionTimeoutError
dnspython==2.7.0      # required by pymongo to resolve mongodb+srv:// URIs; 1.16 is broken on 3.14
certifi==2025.1.31    # trusted CA bundle so the Atlas TLS handshake succeeds in the cloud venv
python-dotenv==1.2.2
